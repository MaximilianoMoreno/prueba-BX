(function () {
  'use strict';
  
  var service =  {
    "conver": convert
  };
  var  mongoose = require('mongoose'),
      _ = require('lodash');
  
  
  module.exports = service;
  
  function convert(baseCurrency, toCurrency) {
    let response = {};
    let base = {base: baseCurrency};
    let rates = {lastName: 'Stark'};
    let person = Object.assign(first, last);
    
    
  }
  
 
  
  
})();
